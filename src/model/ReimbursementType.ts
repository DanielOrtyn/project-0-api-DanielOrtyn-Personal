
export interface ReimbursementType {
    // private static idCounter = 0;
    typeID: number;
    type: string;

    // constructor(newStatus = `Default`) {
    //     this.typeID = ReimbursementType.idCounter++;
    //     this.type = newStatus;
    // }
}